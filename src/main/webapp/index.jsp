<%--
<%@ page import="ir.parliran.App" %>
<%
    if (request.getSession().getAttribute(App.SIGNIN_VAR_NAME) != null) {
        response.sendRedirect(request.getContextPath() + "/file");
        System.out.println("redirect by index.jsp");
    }
    else {
        response.sendRedirect(request.getContextPath() + "/user/signin");
    }
%>--%>
